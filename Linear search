#include<stdio.h>
int main()
{
	int a[]={4,3,2,6,5};
	int i,ele=2,n=5,found=0;
	for(i=0;i<n;i++)
	{
		if(a[i]==ele)
		{
			printf("element found at index:%d:",i);
			found=1;
		}
	}
	if(found==0)
	{
		printf("element not found:");
	}
}
